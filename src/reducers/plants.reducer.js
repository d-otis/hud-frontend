const INITIAL_STATE = [
  {
    name: "Lily",
    latin_name: "Lily-bear"
  }
]

const plantsReducer = ( state = INITIAL_STATE, action ) => {
  switch(action.type) {
    default:
      return state
  }
}

export default plantsReducer